# Ops Compatibility Wrappers

Canonical hosting and deployment scripts live under `projects/ops/scripts/`.

This directory is retained for command-path compatibility only. New ops work
should be added to the `projects/ops` projection.
