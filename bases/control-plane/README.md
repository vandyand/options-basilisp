# Base: control-plane

Purpose:

- validate, inspect, and activate control-plane manifests

Expected composition:

- control-plane protocol and file-store components
- validation components
- observability components where needed
